server:
  port: 9002

spring:
# ActiveMQ

  activemq:
    user: ${ACTIVEMQ_USER}
    password: ${ACTIVEMQ_PASSWORD}
    broker-url: ${ACTIVEMQ_URL}
    packages:
      trust-all: true

# MongoDB

  data:
    mongodb:
      uri: mongodb+srv://${MONGODB_USER}:${MONGODB_PASS}@${MONGODB_HOST}/memberservice?retryWrites=true